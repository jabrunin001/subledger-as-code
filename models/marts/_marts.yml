version: 2

models:
  - name: dim_account
    columns:
      - name: account_id
        data_tests: [not_null, unique]
  - name: dim_loan
    columns:
      - name: loan_id
        data_tests: [not_null, unique]
